eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9100/eureka/
spring:
  application:
    name: common-service2
  cloud:
    config:
      uri: http://localhost:9200
      name: cloud-config
      profile: service2,common


#      management.security.enabled=false
management:
  security:
    enabled: false